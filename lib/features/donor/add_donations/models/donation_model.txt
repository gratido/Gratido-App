// lib/features/donor/add_donations/models/donation_model.dart

class Donation {
  final String donorName;
  final String phone;
  final String pickupLocation;
  final String pickupWindow;

  // Food info
  final String category;
  final int quantity;

  // MULTIPLE PHOTOS
  final List<String> photoPaths;

  // New fields
  final String? preparedTime; // "1–3 hours ago" chips
  final String? expiryTime; // time picker result
  final String? notes; // optional extra notes

  final bool hygieneConfirmed;

  final DateTime createdAt;

  Donation({
    required this.donorName,
    required this.phone,
    required this.pickupLocation,
    required this.pickupWindow,
    required this.category,
    required this.quantity,
    required this.photoPaths,
    required this.hygieneConfirmed,
    this.preparedTime,
    this.expiryTime,
    this.notes,
    DateTime? createdAt,
  }) : createdAt = createdAt ?? DateTime.now();
}
